name: Inject Kiem Do repeat hit patch

on:
  push:
    branches: [main]
    paths:
      - .github/workflows/inject-find-repeat-hit.yml

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Validate and inject repeat hit patch
        run: |
          node --check find-repeat-hit-patch.js
          python3 - <<'PY'
          from pathlib import Path
          p=Path('index.html')
          s=p.read_text(encoding='utf-8')
          tag='<script src="find-repeat-hit-patch.js?v=20260712a"></script>'
          if tag not in s:
              s=s.replace('</body>',tag+'\n</body>',1)
              p.write_text(s,encoding='utf-8')
          assert tag in p.read_text(encoding='utf-8')
          PY
          git diff --check
      - name: Commit loader
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add index.html
          if git diff --cached --quiet; then exit 0; fi
          git commit -m "Load Kiem Do repeat hit patch"
          git push
