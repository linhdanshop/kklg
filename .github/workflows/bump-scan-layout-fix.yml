name: Bump Quet Ma layout fix

on:
  push:
    branches: [main]
    paths:
      - .github/workflows/bump-scan-layout-fix.yml

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Validate and bump layout patch
        run: |
          node --check scan-return-request-patch.js
          python3 - <<'PY'
          from pathlib import Path
          p=Path('index.html')
          s=p.read_text(encoding='utf-8')
          old='scan-return-request-patch.js?v=20260711a'
          new='scan-return-request-patch.js?v=20260711b'
          if old in s:
              s=s.replace(old,new,1)
          elif new not in s:
              raise SystemExit('scan return patch loader not found')
          p.write_text(s,encoding='utf-8')
          PY
          git diff --check
      - name: Commit cache bump
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add index.html
          if git diff --cached --quiet; then exit 0; fi
          git commit -m "Bump Quet Ma layout fix version"
          git push
